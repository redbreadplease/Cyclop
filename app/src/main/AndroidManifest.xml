<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.redbreadplease.cyclop">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />

    <application
        android:icon="@drawable/ic_m_logo_eye_violet"
        android:label="Cyclop"
        android:logo="@drawable/ic_m_logo_eye_violet"
        android:roundIcon="@drawable/ic_m_logo_eye_violet"
        android:screenOrientation="sensorPortrait"
        android:supportsRtl="true">
        <activity
            android:name=".activities.VRActivity"
            android:theme="@style/VRActivity" />
        <activity
            android:name=".activities.NewsActivity"
            android:theme="@style/NewsBarActivity" />
        <activity
            android:name=".activities.GalleryActivity"
            android:theme="@style/GalleryActivity" />
        <activity
            android:name=".activities.VRMenuActivity"
            android:theme="@style/GalleryActivity" />
        <activity
            android:name=".activities.AppMenuActivity"
            android:theme="@style/NewsBarActivity" />
        <activity
            android:name=".activities.SplashActivity"
            android:theme="@style/SplashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>