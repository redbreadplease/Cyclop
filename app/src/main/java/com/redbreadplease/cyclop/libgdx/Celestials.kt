package com.redbreadplease.cyclop.libgdx

import com.badlogic.gdx.graphics.g3d.ModelInstance
import com.badlogic.gdx.graphics.g3d.utils.ModelBuilder

private const val horizonWidth: Float = 8f
private const val circlePartsAmount = 12


fun getStarsModelsInstances(modelBuilder: ModelBuilder): MutableList<ModelInstance> {
    val starsModelsInstances = mutableListOf<ModelInstance>()

    return starsModelsInstances
}
